import send from '@polka/send-type'; // Utilisation: send(res, status, data);

// TODO exporter une fonction `get` qui renvoie un message bidon

// TODO export une fonction `post` qui log ce qu'on envoie